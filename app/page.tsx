import { UnderwritingWorkspace } from "./components/UnderwritingWorkspace";

export const metadata = {
  title: "CredIntel AI | Underwriting workspace",
  description: "Explainable, real-time credit decisions for new-to-credit applicants.",
};

export default function Home() {
  return <UnderwritingWorkspace />;
}
